from __future__ import annotations

import streamlit as st


def render_planning_page() -> None:
    st.markdown("## Planning")
    st.info(
        "Placeholder. Future phase: GPX import, route map + profile, and forecast snapshots. "
        "We’ll keep this shell stable while we add the real planning engine."
    )
